<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.muda.dao.get_girl.OauthsDao">
    <!--增-->
    <insert id="save" useGeneratedKeys="true" keyProperty="id">
        insert into oauths(project_name,oauth_url,clientId,cientSecret,grantType,scene,update_time)
        VALUES(#{projectName},#{oauthUrl},#{clientId},#{cientSecret},#{grantType},#{scene},#{updateTime});
    </insert>

    <!--删-->
    <delete id="delete">
        DELETE from `oauths` WHERE id = #{id}
    </delete>
    <!--删【批量】-->
    <delete id="batchRemove">
        DELETE from `platform_plan_case` WHERE id in
        <foreach collection="array" item="ids" open="(" separator="," close=")">
            #{ids}
        </foreach>
    </delete>


    <!--改-->
    <update id="update">
        UPDATE `oauths`
        <set>
            <if test="projectName !=null">project_name = #{projectName},</if>
            <if test="oauthUrl !=null">oauth_url = #{oauthUrl},</if>
            <if test="clientId !=null">clientId = #{clientId},</if>
            <if test="cientSecret !=null">cientSecret = #{cientSecret},</if>
            <if test="grantType !=null">grantType = #{grantType},</if>
            <if test="scene != null">scene = #{scene},</if>
            <if test="updateTime != null">update_time = #{updateTime}</if>
        </set>
        where id = #{id}
    </update>


    <!--查-->
    <select id="get" resultType="com.muda.beans.get_girl.Oauths">
        select * from `oauths` where project_name LIKE CONCAT('%',#{projectName},'%')
    </select>

    <!-- 用于查询planName -->
    <select id="findByName" resultType="java.lang.String">
        SELECT project_name from oauths;
    </select>
</mapper>